:::page{template="chapter-opener"}
# Welcome

This is a starter document for Tender.
:::

:::page
# Editing this document

Open `content.md` in your editor and edit it. Run `tender preview` to see live updates, or `tender build` to produce a PDF.

:::callout{variant=info}
Components like this `callout` are declared in `project.yaml` and styled in `styles.css`.
:::

:::row{label="Tip"}
Use the `row` template to put a label or note in a margin column next to the body content.
:::
:::
